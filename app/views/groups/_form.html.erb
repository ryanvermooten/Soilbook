<%= simple_form_for(@group) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :Name %>
    <%= f.input :Area %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
